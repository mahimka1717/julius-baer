{
  "name": "julius baer",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm run generate-images && vite",
    "build": "pnpm run generate-images && vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "generate-images": "node generate-images.js"
  },
  "devDependencies": {
    "handlebars": "^4.7.8",
    "sass": "^1.89.0",
    "sass-color-helpers": "^2.1.1",
    "sharp": "^0.34.3",
    "terser": "^5.40.0",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "gsap": "^3.13.0",
    "lazysizes": "^5.3.2",
    "vite-plugin-handlebars": "^2.0.0"
  }
}
